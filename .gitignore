# Foundry build output (dependencies come from git submodules, see .gitmodules)
deploy/out/
deploy/cache/
deploy/foundry.lock

# Dry-run broadcasts (simulation only); real broadcasts are kept as a deployment record
deploy/broadcast/*/*/dry-run/

# Secrets — never commit
.env
.env.*
*.key
